# pt_prototyping

Standalone Flutter sandbox for UI/UX prototyping. This is not part of the shipped product — see [PriorityTaskManager.Flutter](../PriorityTaskManager.Flutter) for the actual client.

Setup: `flutter pub get` (see the root [README.md](../README.md) for Flutter SDK version notes), then `flutter run`.
